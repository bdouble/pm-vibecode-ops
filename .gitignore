# Claude Code configuration
CLAUDE.md
.claude/

# Git worktrees (created per-ticket)
.worktrees/

# Swarm scratch: per-ticket worktrees, epic integration worktrees, locks, run state,
# and local run forensics. Per-machine, never committed (same rule the workflow ensures
# in target repos).
.swarm/

# macOS
.DS_Store
# Obsidian (personal note-taking config)
.obsidian/

# Internal research and context (not for public)
context/
.context/
